- hosts: ca
  roles: [ca]

- hosts: webserver
  roles: [webserver]

- import_playbook: ansible/hosts/webserver.yml  # generates key + CSR
- import_playbook: ansible/hosts/ca.yml          # signs CSR, fetches crt
- import_playbook: ansible/hosts/webserver.yml   # deploys cert + nginx config